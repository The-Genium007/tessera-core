# server/ — Host autoritaire (Rust)

Serveur de simulation autoritaire. Phase 0-A : squelette (workspace + tick rate).
Phases suivantes : transport TCP→QUIC, ECS, RP, persistance.

- Build : `cargo build -p server`
- Test : `cargo test -p server`
- Run : `cargo run -p server`
