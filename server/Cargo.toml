[package]
name = "server"
version = "0.0.1"
edition.workspace = true
license.workspace = true

[features]
default = []
gns = ["dep:game-networking-sockets"]

[dependencies]
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
protocol = { path = "../protocol" }
tessera-authority = { path = "../../tools/authority-tessellation" }
flatbuffers = "=25.12.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
game-networking-sockets = { version = "=0.2.0", optional = true }
jsonwebtoken = "9"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
redis = { version = "0.27", features = ["tokio-comp", "connection-manager"] }
sqlx = { version = "0.7", features = ["runtime-tokio", "tls-rustls", "postgres", "migrate"] }
base64 = "0.22"

[dev-dependencies]
tempfile = "3"
rsa = { version = "0.9", features = ["pem"] }
rand = "0.8"

[lib]
path = "src/lib.rs"

[[bin]]
name = "server"
path = "src/main.rs"
